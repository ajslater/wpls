services:
  ci:
    build:
      context: .
      target: wpls-ci
    image: wpls-ci:ci
    container_name: wpls-ci
    volumes:
      - ./test-results:/app/test-results
      - ./dist:/app/dist
    environment:
      - PYTHONUNBUFFERED=1
    entrypoint: [sleep, infinity]
