services:
  ci:
    build:
      context: .
      target: wpls-ci
    container_name: wpls-ci
    entrypoint:
      - sleep
      - infinity
    environment:
      - PYTHONUNBUFFERED=1
    image: wpls-ci:ci
    volumes:
      - ./test-results:/app/test-results
      - ./dist:/app/dist
